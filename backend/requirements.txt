fastapi>=0.115.0
uvicorn[standard]>=0.33.0,<0.34.0
aiofiles>=24.1.0
jinja2>=3.1.0
pydantic>=2.0.0
PyYAML>=6.0
reportlab>=4.0      # PDF report (pure-Python, no headless browser)
matplotlib>=3.7     # report figures
pillow>=10.0        # image sizing in the PDF
openpyxl>=3.1       # NCBI templates + single-column stats workbook
pandas>=2.0         # Excel metadata parsing
biopython>=1.81     # NCBI Entrez (eutils) + FASTA parsing
requests>=2.31      # eutils HTTP
